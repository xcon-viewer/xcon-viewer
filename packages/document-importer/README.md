# @xcon-viewer/document-importer

Document-to-XCON Markdown importer for Template Lab.

This package adapts `kordoc.parse()` output into Markdown that can include
XCON/SKETCH, XCON Chain SUGAR, and XCON Workflow fences.

